import items from './items.fixtures.js';
import isValidItem from '..utils/checkItems.js'

describe('Check item', () => {
  test('Has name', () => {

  }),

  test('Has no name', () => {

  });

  test('has content with more 1000 chars', () => {

  });

  test('without unique name', () => {

  });

  test('without creation date', () => {

  });
});